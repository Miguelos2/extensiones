function extractText() {
  const bodyText = document.body.innerText;  // Extrae el texto visible de la página
  return bodyText;
}

// Ejecuta la función de extracción cuando sea necesario
extractText();
